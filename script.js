// Função para mostrar ou ocultar os detalhes de cada prato
function toggleDetails(detailId) {
    const detailElement = document.getElementById(detailId);
    detailElement.classList.toggle('show');
}
